@props([
    'title',
    'description',
])

<div class="text-center mb-4">
    <h4 class="fw-bold font-outfit text-body mb-1">{{ $title }}</h4>
    <p class="text-secondary small mb-0">{{ $description }}</p>
</div>
